<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="">

    <resultMap id="CaseMap" type="com.codeloong.infrastructure.persistent.po.">
        <id column="id" property="id"/>
        <result column="" property=""/>
        <result column="" property=""/>
    </resultMap>

    <insert id="insert" parameterType="com.codeloong.infrastructure.persistent.po.">
    </insert>

    <update id="update" parameterType="com.codeloong.infrastructure.persistent.po.">
    </update>

    <select id="queryEmployeeByEmployNumber" parameterType="java.lang.String" resultMap="CaseMap">
        SELECT a, b, c
        FROM table
        WHERE a = #{a}
    </select>

</mapper>